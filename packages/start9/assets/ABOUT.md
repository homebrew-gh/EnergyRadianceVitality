# ERV package assets

Static files bundled into the StartOS package:

- `nostr-rs-relay.svg` / `haven.svg` — dependency icons in the service manifest
- `screenshot-setup.svg` / `screenshot-shell.svg` — illustrations for `instructions.md`

These are package metadata assets, not mounted into the ERV container.
